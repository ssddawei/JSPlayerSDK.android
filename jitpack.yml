jdk:
  - openjdk9
before_install:
   - echo ./custom_setup.sh
install:
   - echo "Running a custom install command"
   - mkdir -p ~/.m2/repository/
   - cp *.aar ~/.m2/repository/
env:
   MYVAR: "custom environment variable"
